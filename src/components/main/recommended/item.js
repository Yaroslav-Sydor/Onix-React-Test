import '../../../styles/main/recommended/item.css';

function Item() {
    return (
        <div className="recommended__item__container">
            <div className={'recommended__img__wrapper'}>
                <img className={'recommended__img'} src={''} alt={''}/>
            </div>
            <div className={'recommended__information__wrapper'}>

            </div>
        </div>
    );
}

export default Item;
